# Test dependencies — installed in CI and locally, NOT in Railway production.
# Production image only installs requirements.txt (no pytest, no test-only libs).

-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
respx>=0.21
